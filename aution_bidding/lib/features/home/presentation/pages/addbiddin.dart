import 'package:flutter/material.dart';

class AddBidding extends StatelessWidget {
  const AddBidding({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Bidding'),
      ),
    );
  }
}